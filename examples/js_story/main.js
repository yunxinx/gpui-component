// JavaScript counterpart to crates/story. The shell entry point consumes this
// default export when the component-shell composition is available.
import StoryGallery from "./app.js";

export default StoryGallery;

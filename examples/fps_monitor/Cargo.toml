[package]
name = "fps_monitor"
description = "A performance HUD overlaid on an adjustable animation load, using gpui-fps."
version = "0.1.0"
publish = false
edition.workspace = true

[dependencies]
gpui.workspace = true
gpui_platform.workspace = true
gpui-fps.workspace = true

[lints]
workspace = true
